require 'spec_helper'

describe Profile do
  # check that all methods respond

  # check that has_one, belongs_to works properly

  # create profile (need to create user first)
  # check that fields validate correctly
end
